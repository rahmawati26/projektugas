var  tombolMenu = dokumen.getElementsByClassName('tombol-menu')[0];
var  menu = dokumen.getElementsByClassName('menu')[0];

tombolMenu.klik = fungsi() {
    menu.daftarkelas.toggle('aktif');
}

menu.klik = fungsi() {
    menu.daftarkelas.toggle('aktif');
}